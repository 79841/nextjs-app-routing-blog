import styled from "styled-components";

const Container = styled.div`
  font-size: 1.5rem;
  font-weight: bold;
`;

const Logo = () => {
  return <Container>TTing</Container>;
};

export default Logo;
